---
date: 2026-09-21
since:
  tafwid: d044ea0d33c457098c7dc682cb85c6c7c8e1bbe9
---
# Documentation audit 2026-09-21

## Undocumented changes

- [x] OpenCode execution, OpenRouter/Zen preflight, native resumes and instruction
  retention: covered by architecture, the OpenCode reference and provider trials.
- [x] Free-model scouting: covered by README, the scout skill and architecture;
  recommendations are capability candidates rather than measured quality scores.
- [x] Dashboard tokens, costs and effective throughput: covered by README and
  the dashboard reference, including filter/session scope and missing data.
- [x] LM Studio/vLLM connections: covered by the local-model reference,
  architecture and local-inference trial. Machine-specific operational values
  remain in HomeLab's private environment documentation.
- [x] Trial reports are now linked from README so they can be discovered.

## Stale docs

- [x] Dashboard grouping/liveness descriptions still named only Claude; corrected
  to describe harness/native-session identity and worker processes.
- [x] README separates explicit local selection from Claude-only saved routing.
  No claim that the dashboard can configure local connections was added.
- [x] Zen trial remains explicit that live resumed execution is unverified.
  A later authenticated attempt also timed out without usable events; it did
  not establish successful authenticated execution or resolve the earlier 403.

## Missing ADRs (candidates)

- [x] Added [ADR-0001](../decisions/0001-opencode-worker-harness.md) for choosing
  OpenCode over a new tool loop or per-provider harnesses, and for preserving
  explicit provider/credential/acceptance boundaries.

## Validation

Package validation and the full offline suite passed: 159 Python and 14
JavaScript tests. Apple's `make` launcher stopped at its Xcode license check;
its three recipe commands were run directly with the same source and passed.
A headed local browser check at desktop (1440×1000) and mobile (390×844)
verified grouped resumed runs, summary metrics and per-run Usage details using
synthetic records. No horizontal document overflow was observed. The only browser
console error was the missing optional favicon. Screenshots remain private.
The existing real provider trials were reviewed, not repeated during this audit.
No GPT savings percentage or general model-quality claim is supported.

## Coverage note

No prior audit existed. Scope follows the user's request to document and commit
this task: all pending Tafwid changes since the recorded HEAD, with its three
existing commits inspected for context. This is not a claim that HomeLab's
unrelated work was audited. Tafwid retains its existing public documentation
layout; Archivist's single-home, decision-record and audit conventions were
applied without moving runtime skill references into a second documentation tree.
Private transcripts, credentials, local connection state and screenshots are
excluded from the commit. The changes are locally installed but not released
or pushed by this audit.
